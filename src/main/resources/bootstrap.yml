spring:
   cloud:
    compatibility-verifier:
      enabled: false
    config:
      enabled: true
      uri: http://localhost:9296echo "# department-service" >> README.md


      spring:
        cloud:
          compatibility-verifier:
            enabled=false